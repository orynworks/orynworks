{
  "name": "@oryn/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "fmt": "forge fmt",
    "deploy:sepolia": "forge script script/Deploy.s.sol --rpc-url ${BASE_SEPOLIA_RPC:-https://sepolia.base.org} --broadcast --verify --etherscan-api-key $BASESCAN_API_KEY",
    "deploy:mainnet": "forge script script/Deploy.s.sol --rpc-url ${BASE_RPC:-https://mainnet.base.org} --broadcast --verify --etherscan-api-key $BASESCAN_API_KEY"
  }
}
